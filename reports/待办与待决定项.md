# 待办与待决定项（截至本次会话）

> 状态：`main @ 87c7573` · 647/647 测试绿 · watch 在跑（pwsh-14）· 无队友在跑

---

## A. 已定案（不用再讨论，直接做）

| # | 事项 | 决定 |
|---|---|---|
| D1 | `entry_edit` + `entry_meta` | **合并**成 `entry_update`（world + uid 必填，其余可选） |
| D2 | `entry_update` 能否整条替换 content | **允许**（与 old_string/new_string 二选一） |
| D3 | 高级字段是否全进 schema | **全进**（probability / group / sticky / cooldown / match_whole_words） |
| D4 | `wb_preview` 试算工具 | **不做**（用户：世界书触发规则很简单，不必单独测） |
| D5 | 脚本沙箱 / 脚本工具 | **不做**（沙箱验不了真环境；MVU 脚本编写以后再说） |
| D6 | 立绘工具 | **保留完整操作**：一个 list + 一个 prompt（见 B5 修正） |
| D7 | 内置 skill | **A 方案**：删掉「世界书精修」「势力关系梳理」，换成「如何写技能」的结构参考 |
| D8 | 提示词导出粒度 | **B 方案**：分三类各导各的（工具提示词 / 预设 / 技能） |
| D9 | 世界书 skill 结构 | 树状：浅层正文 + `references/` 按需读 |
| D10 | 环境认知放哪 | **不强制挂载**；靠「可用技能」段的引导语，模型自己调 `skill()` 读 |

---

## B. 待办（按风险排序）

### 🔴 会毁数据 / 已毁过一次

| # | 事项 | 现状 |
|---|---|---|
| B1 | **世界书写路径真机端到端**（task-36） | 读路径真机已通；写路径只到代码层+单测。上次就是这里清空了 `Eldoria`。**必须用自建一次性书** |
| B2 | **备份/回滚四条硬性质测试**（task-31） | 未写。`extra` 字段丢了比不备份更坏 |
| B3 | 写入后回读校验（新增） | 上次界面显示「换回去 4 条」实际 0 条 —— **界面在替底层撒谎** |

### 🟠 你这次指出的问题（都已核实为真）

| # | 事项 | 核实结果 |
|---|---|---|
| B4 | **世界书没默认勾当前激活的书** | 真 bug：`worldbook_names` 默认 `[]`；`WorldbookPort.current()` 已有能力但**没接** |
| B5 | **立绘工具违背初衷** | 真错：`portrait_meta` 和 `portrait_prompt` 读的是同一份数据，该合成一个；**批量提取没做** |
| B6 | **JSON 输出没做成工具** | 缺失：`makeArtifact` 是被动的；没有 `write_file` / `read_file` 工具 |
| B7 | **内置提示词改了会丢** | 机制错：`applyBuiltins()` 只补缺失 id，**不覆盖**，改内置默认老用户看不到 |
| B8 | **内置 skill 没价值** | 内容错：那两本讲写作风格（压到 150 字、→盟友→ 符号），是你的活儿不是我的 |
| B9 | **agent/普通预设区分是死代码** | 真死：`session.mode` 全仓库**无人读**，只有 UI 显示 |

### 🟡 阶段 7 未验完

| # | 事项 |
|---|---|
| B10 | 375px 真机宽度（只验了逻辑，没验窄屏） |
| B11 | 失败路径界面表现：语法错 / apiVersion 不匹配 / 撞内置 id |
| B12 | 越界 id、重复安装的健壮性 |
| B13 | HTTPS 下 http 源被混合内容拦（那时引导用「粘贴代码」） |
| B14 | **独立验收没交付**（task-48）：验收员两次没交出报告，等于阶段 7 缺一道独立闸 |

### 🟢 世界书这轮的正题

| # | 事项 |
|---|---|
| B15 | 世界书工具改造：合并 `entry_update`、全进字段、**死条目拦截** |
| B16 | `wb_search` / `wb_read` 结果带**激活状态**（常亮/关键词/已停用） |
| B17 | 写 `世界书工程` skill（正文 + `references/字段速查表.md` + `references/范式模板.md`） |
| B18 | 写 `酒馆环境` skill（浅层，细节拆 references/） |
| B19 | **「可用技能」段引导语加强**（现在太弱，模型可能根本不读 skill） |
| B20 | 预设系统提示词里的「苍玄界」残留 → 改掉 |

### ⚪ 基础设施（为「你调完发我」这条流程）

| # | 事项 |
|---|---|
| B21 | `write_file` / `read_file` 工具（模型命名 + 拓展名；`.json`/`.md`/`.txt` 可用，`.js`/`.py` 被 ST 挡） |
| B22 | 提示词导出/导入：工具提示词 / 预设 / 技能 **分三类**（D8） |
| B23 | 内置内容可更新：版本号 + 用户改过则保留 + 一键恢复默认 |
| B24 | 立绘工具三合一：`portrait_list` + `portrait_read(names[])` |
| B25 | 删死控件「Agent｜聊天」开关 |

### ⚫ 没开始 / 以后

| # | 事项 |
|---|---|
| B26 | 正则工具（`regex_list` / `regex_update` / `regex_test` 离线试跑）—— **待你确认是否保留** |
| B27 | 阶段 8：隔离 iframe、hook bus、`contributes.prompts` |
| B28 | 角色卡字段工具（`card_get` / `card_update`） |
| B29 | E1 计划工具（你说过是最值的 DSH 借鉴） |
| B30 | SKILL.md 兼容层（能不能直接吃 TavernWeave 的 22 个 skill） |
| B31 | V1-V3 视觉 / F1-F3 交互 / O1-O4 候选（计划报告里，等你挑） |
| B32 | 脚本形态退役（`resolveScriptId` / `scriptScope` / 291 行测试） |

---

## C. 待决定项（我卡在这，需要你一句话）

| # | 问题 | 选项 | 我的倾向 |
|---|---|---|---|
| C1 | **MCP 浏览器直连会不会被 CORS 挡** | 要验吗？决定阶段 6 对普通用户能不能用 | **要验**（几分钟） |
| C2 | **顺序** | 先做哪批？ | B4+B9（bug/死代码）→ B15-B20（世界书正题）→ B21-B25（基础设施） |
| C3 | **正则工具保留吗** | 保留 / 砍掉 | 保留（纯数据 + 能离线试跑，比脚本简单得多） |
| C4 | **B5 立绘工具的确切形状** | 见下 | 待你确认 |

### C4 详述：立绘工具

你的原话：「保留一个 list，一个 prompt，因为模型要列出角色列表，读原生图提示词，llm 转为固定格式人物预设提示词」

我理解成：

```
portrait_list            → 列出角色（名字 + 有没有图 + 来源）
portrait_prompt(names[]) → 批量读原生图提示词（NovelAI/A1111/ComfyUI 元数据里的 prompt）
```

然后**模型自己**把读到的提示词转成「固定格式的人物预设提示词」，再用 `write_file` 落盘（B21）。

**要确认的**：
- `portrait_meta` 是**合并进 `portrait_prompt`**（读提示词时顺带返回元数据），还是**直接删掉**？
- `names[]` 支持一次多个吗？（我理解是的，因为你要「批量提取」）
- 「固定格式」的 schema 谁定 —— 你给，还是我按现有 `readPortraitMeta` 的字段推一版给你改？

---

## D. 我认的错（记录在案，避免重犯）

| # | 错 | 性质 |
|---|---|---|
| 1 | 世界书工具把世界书当「可读写的 JSON 文本」 | 不懂领域就设计工具 |
| 2 | 立绘工具写成「改图用」，违背批量提取初衷 | 没看实际使用场景 |
| 3 | 写 `dev_deploy` 时解析 webpack 日志判断完成 | 拿已知套路套陌生结构（差点造成白屏） |
| 4 | dev 构建污染单文件脚本产物 | 同上（真造成了测试红） |
| 5 | 想自己写「身份层」提示词 | 该借鉴人类已有产物，不该凭空造 |
| 6 | 脚本沙箱想成「完整闭环」 | 过度设计 |
